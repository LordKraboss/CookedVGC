# dependencies
**/node_modules

# build output
frontend/dist
**/.vite

# secrets / local env
.env
.env.*
!.env.example

# runtime state (lives in volumes on the server; never commit it)
backend/data/
backend/public/sprites/

# logs
*.log

# stray local dev artifacts at repo root (anchored to root so they don't
# match real files like frontend/index.html)
/dex.json
/*.html
/learnsets_temp.json
/{backend/
/{src/
/data/

# internal docs — kept local, not published to the public repo
/CLAUDE.md
/claude-workflow.md

# OS / editor
.DS_Store
Thumbs.db
