The session is ending. Update CLAUDE.md and memory files to reflect what changed.

Steps:
1. Read the current `CLAUDE.md`
2. For each structural change made this session, update the relevant section:
   - New file → add to file map
   - New/changed route → update route table
   - New status, lifecycle change, or invariant → update that section
   - New shared component or changed props → update shared components section
   - New convention → add to conventions section
3. Keep every entry to one line. No narrative. No changelog. Only permanent facts.
4. If a memory file in `C:\Users\InfoWare31\.claude\projects\C--Users-InfoWare31-Desktop-pokemon-vgc\memory\` is stale, update or remove it.

Only update if something structural actually changed. A bug fix or style tweak with no API/file/pattern change needs no update.
